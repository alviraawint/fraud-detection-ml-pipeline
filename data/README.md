# Data Folder

Place the dataset file here:

```text
data/creditcard.csv
```

Expected target column:

```text
Class
```

Target values:

- `0` = normal transaction
- `1` = fraudulent transaction

The actual CSV file is usually not committed to GitHub because it may be large and may have dataset licensing restrictions.
