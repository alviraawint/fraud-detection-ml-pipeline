# Models Folder

This folder stores trained model artifacts generated by the pipeline.

After running:

```bash
python main.py
```

the best model will be saved as:

```text
models/best_model.pkl
```

The best model is selected based on PR-AUC, which is useful for imbalanced fraud detection datasets.
